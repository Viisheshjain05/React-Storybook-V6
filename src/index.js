// Todo Global
// Add Font color sizes, ...rest for all the typography
// Add The Viewport and media queries for all screen sizes
// Layout the best possible structure to get better views
// Remove Css and add styled-Components.

// Create A Npm Package to Get all the Components From there not copping into other projects

import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
